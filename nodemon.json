{
  "watch": [
    "src"
  ],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && hb-service run -U ./test/hbConfig --stdout 2>&1 | tee ./test/hbConfig/homebridge.log",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}